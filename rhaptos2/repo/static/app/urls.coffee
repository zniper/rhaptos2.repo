define [], ->
  return {
    # in `Auth`
    ME: '/me'
    # in `Models`
    CONTENT_PREFIX: '/content/'
    WORKSPACE: '/workspace/'
    # in `Views`
    KEYWORDS: '/keywords/'
    USERS: '/users/'
  }
